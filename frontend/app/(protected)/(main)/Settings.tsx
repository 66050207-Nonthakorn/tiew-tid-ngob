import { Text } from "@/components/ui/Text"
import { SafeAreaView } from "react-native-safe-area-context";

export default function SettingTab() {
  return (
    <SafeAreaView>
      <Text className="text-6xl/tight">
        Setting 3
      </Text>
    </SafeAreaView>
  );
}
